# Quelle commande permet d'ajouter un utilisateur local supplémentaire ? [Commande]
adduser the-user-name

# We can also use the following, but it's less user-friendly:
# useradd <options> <username/login>
