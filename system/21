# Listez tout les utilisateurs réels de la machine [Commande]
cat /etc/passwd | grep '/home/' | awk -F ':' '{print $1}'

# /etc/passwd stores all the users of the machine.
# The "real" (human rather than system) users are available under /home/
