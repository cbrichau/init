# Récupérez le path complet du fichier dans lequel est écrit
# l'adresse IP du serveur DNS que vous utilisez [Explication]
/etc/resolv.conf

# The file /etc/resolv.conf contains the DNS address of our server:
# "cat /etc/resolv.conf" ==>
# --- 
# domain s19.be
# nameserver 10.51.1.1
# ---
